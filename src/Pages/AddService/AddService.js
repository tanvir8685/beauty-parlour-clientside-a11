import React from 'react';
import useTitle from '../../hooks/useTitle';

const AddService = () => {
    useTitle('addService')
    return (
        <div>
            <form action=""></form>
            <button o></button>
        </div>
    );
};

export default AddService;